.SideBar
  .TopItem
    .TopItem__list
      %h3.TopItem__Name
        = current_user.name
      %ul.TopItem__iconList
        %li.TopItem__Icon
          = link_to new_group_path do
            = icon('fas', 'edit')
        %li.TopItem__Icon
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')

  .Group
    .Group__list
    - current_user.groups.each do |group|
      .grpop
        = link_to group_messages_path(group) do
          .Group__name
            = group.name
          .Group__massage
            まだメッセージがありません
